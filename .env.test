# Test environment configuration
# Uses local Postgres service (no SSL required)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taskdb
DATABASE_SSL=false
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=7d
NODE_ENV=test
OPENAI_API_KEY=
